<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:be="clr-namespace:MVVM.Exemplo.Behaviors"
             mc:Ignorable="d"
             x:Class="MVVM.Exemplo.View.MainPage"
             Title="Main">
    <ContentPage.Content>
        <StackLayout 
            Padding="20">
            <ListView ItemsSource="{Binding Usuarios}" 
                      SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Nome}" Detail="{Binding SobreNome}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.Behaviors>
                    <be:EventToCommandBehavior EventName="ItemTapped" Command="{Binding TapCommand}"/>
                </ListView.Behaviors>
            </ListView>
            <Button 
                Text="Add"
                Command="{Binding AddCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>